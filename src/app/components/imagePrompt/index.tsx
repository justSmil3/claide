"use client"

const ImagePrompt = ({className}:{
    className:string;
}) => {
    return(
        <div className={`${className}`}>
            <div className="w-full h-full bg-teal-400 rounded-2xl">

            </div>
        </div>
    );
};

export default ImagePrompt;